Labels a group inside a screen.

```jsx
<SectionHeader title="Transaksi" meta={`${data.transactions.length} transaksi`}
  action={<Button variant="ghost" size="sm">Semua</Button>} />
```

18px/600 sits deliberately below the 24px/700 screen title — never use two 24px headings on one screen.

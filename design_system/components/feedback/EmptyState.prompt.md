Zero-data state. The only component that uses a visible hairline outline instead of a filled surface.

```jsx
<EmptyState icon={<Icon name="inbox" size={22} />} title="Belum ada transaksi"
  description="Begitu ada email notifikasi dari BCA atau Jago, transaksinya muncul di sini otomatis." />
```

Copy stays matter-of-fact and explains the automation — no exclamation marks, no illustrations.
